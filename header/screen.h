#pragma once
typedef enum
{
    HOME = 0,           // Home menu
    GAME,               // Game menu
    CONTINUE,           // Continue menu (=== Game menu)
    LOGIN,              // Login menu
    HIGHSCORE,          // Ranking menu
    INSTRUCTION,        // Instruction menu
    EXIT
} Screen;